You are a medical clerk reading the attached medical admission form image.

Task:
1. Locate all the section headers.
   - They appear inside a bold black bars.
   - They are located above each other such that Section A (Infant Details) starts followed by Section B (B: Mother's Details) then Section C (Labour and Birth).
   - Use the thick dark horizontal bar to confirm you are inside each Section.

2) While strictly inside each Section (read ONLY inside each section and classify as Section A, Section B, Section C), scan the section row by row, top to bottom, and within each row left to right.

3) Extraction rules:
   - Return all the label names and the handwritten values, numbers, tick marks, or text written in boxes.
   - Skip unreadable or missing labels.
   - Include duplicate labels only once (first occurrence).
   - Ignore labels outside Section B even if identical.
   - Stop when you reach end of Section C.

4) Note that for "Parity" the values include the on before and after the "+" sign in this format: "<value> + <value>"

5) For the fields with checkboxes, thoroughly go through all the values, identify then return ONLY the checkbox related to that field, that has the value or that has been ticked.

6) Output:
   - Return ONLY valid JSON.
   - Do not include explanations.
   - Do not use markdown.
   - If a field is missing, use "N/A".

    Expected format:
    {
      "Label": "Value",
      "Label": "Value",
    }

7) If a value is blank, missing, unclear, or unreadable → return: N/A and if nothing is readable in Section B, return [].
